"""Слайс правки записи: pending-режим «✏️ Править» и его контракты (S3).

Правится ТОЛЬКО текст типизированной записи (note/idea/decision/question —
text; task — title). CaptureEvent неизменяем: оригинал первой версии навсегда
в журнале, отдельной истории версий нет (спека §3.1). Время при правке НЕ
пере-извлекается, классификатор НЕ перезапускается — правка это явное действие
человека, единственное производное следствие — пере-индексация.
"""
